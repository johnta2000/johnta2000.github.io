{
  "scripts": {
    "build:lineup": "node scripts/build-native-lineup.mjs",
    "test:rally": "node --test tools/rally/*.test.cjs",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy",
    "monitor:hertz": "node scripts/monitoring/hertz-las-may-2027.mjs",
    "test:monitoring": "node --test scripts/monitoring/*.test.mjs"
  },
  "dependencies": {
    "convex": "^1.41.0"
  },
  "overrides": {
    "esbuild": "0.28.1"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "playwright": "^1.55.0"
  }
}
